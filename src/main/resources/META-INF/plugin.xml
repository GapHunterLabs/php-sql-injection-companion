<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.phpsqlinjectioncompanion</id>
    <name>PHP SQL Injection Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning on <code>mysqli_query(</code>, <code>->query(</code>,
        or <code>->exec(</code> (the <code>mysqli</code>/<code>PDO</code>
        query methods) whose SQL string argument contains
        <code>$variable</code> interpolation or a concatenation with a
        variable, rather than a prepared-statement placeholder. This
        is the textbook SQL injection anti-pattern: prepared statements
        exist precisely so the SQL text and the data are sent to the
        database separately -- string-building a query defeats that
        protection entirely.</p>
        <p>Confirmed real gap: "PHP Inspections (EA Extended)" (one of
        the most widely used PHP inspection plugins on Marketplace)
        covers <code>unserialize()</code>, weak crypto,
        <code>extract()</code>/<code>parse_str()</code>, and several
        other security patterns, but not SQL injection through query
        method parameters -- confirmed by reading its own documented
        security feature list before building this.</p>
        <p><b>100% static text analysis</b> -- a regex-based line
        scanner, not a real PHP parser, so it works whether the PHP
        plugin is installed or not.</p>
        <p>Stated honestly: v0.1 doesn't trace whether the interpolated
        value actually originates from untrusted input, so
        interpolation of a hardcoded constant is a possible (rare)
        false positive. A call whose argument is entirely a static
        literal, or built with <code>?</code>/named placeholders and a
        separate <code>bind_param</code>/<code>execute</code> call, is
        correctly never flagged.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <localInspection language=""
            groupName="PHP SQL Injection Companion"
            displayName="Interpolated/concatenated SQL query string"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.gaphunter.phpsqlinjectioncompanion.inspection.SqlInjectionInspection"/>
        <notificationGroup id="PHP SQL Injection Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
